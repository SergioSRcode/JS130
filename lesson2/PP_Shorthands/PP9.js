// Use array and object destructuring to extract the elements 
// and properties from arr and obj into individual variables.

const arr = [1, 2, 3];
const obj = { a: 'a', b: 'b', c: 'c' };

let [ num1, num2, num3 ] = arr;
let { a, b, c } = obj;